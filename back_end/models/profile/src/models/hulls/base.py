# Remove unused import of enum.Enum

# Fix E261: add at least two spaces before inline comments

class BaseModel:
    def __init__(self, value, other, third):
        self.value = value  # Initialize value
        self.other = other  # Initialize other
        self.third = third  # Initialize third